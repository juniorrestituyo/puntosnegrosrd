# Supabase — públicas (van al cliente, OK exponer)
NEXT_PUBLIC_SUPABASE_URL=https://rvonfrijjmzigiywjply.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase — privada (NUNCA al cliente, solo server)
SUPABASE_SERVICE_ROLE_KEY=

# Salt para hashear IPs (rate-limit). Genera string aleatorio largo.
IP_HASH_SALT=

# Secreto para el admin panel (MVP). Cambia esto a Supabase Auth en V2.
ADMIN_SECRET=

# URL pública del sitio (para links permanentes en compartir-con-autoridad)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
